# 🧪 คู่มือการทดสอบสำหรับ Windows

## ⚠️ ปัญหา PowerShell Execution Policy

หากคุณเห็นข้อความแจ้งเตือน "cannot be loaded. The file is not digitally signed" ให้รันคำสั่งนี้ในโหมด Administrator:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

จากนั้นรีสตาร์ท terminal

---

## 🔍 การทดสอบที่ 1: Environment Validation

### กรณีทดสอบที่ 1.1: ไม่มี JWT_SECRET (ควรล้มเหลวใน production)

**ขั้นตอนที่ 1:** สำรองไฟล์ `.env` ของคุณ

```powershell
Copy-Item server\.env server\.env.backup
```

**ขั้นตอนที่ 2:** ลบ JWT_SECRET ชั่วคราว

```powershell
# แก้ไขไฟล์ server\.env และคอมเมนต์บรรทัด JWT_SECRET
# หรือลบออกชั่วคราว
```

**ขั้นตอนที่ 3:** ลองเริ่ม server ในโหมด production

```powershell
cd server
$env:NODE_ENV="production"
npm start
```

**ผลลัพธ์ที่คาดหวัง:** ❌ ควรแสดง error:

```
Error: Missing environment variables: JWT_SECRET
```

**ขั้นตอนที่ 4:** กลับค่าไฟล์ `.env`

```powershell
Copy-Item server\.env.backup server\.env
```

---

### กรณีทดสอบที่ 1.2: ตรวจจับ JWT_SECRET ที่อ่อนแอ

**ขั้นตอนที่ 1:** ตั้งค่า JWT_SECRET ที่อ่อนแอ

```powershell
# ในไฟล์ server\.env ตั้งค่าชั่วคราวเป็น:
# JWT_SECRET=dev-secret
```

**ขั้นตอนที่ 2:** เริ่ม server

```powershell
npm run dev
```

**ผลลัพธ์ที่คาดหวัง:** ⚠️ ควรแสดงคำเตือน:

```
⚠️  WARNING: Using default/weak JWT_SECRET. Change it immediately!
```

---

## 🔍 การทดสอบที่ 2: การตั้งค่าการแจ้งเตือนทางอีเมล

### ข้อกำหนดเบื้องต้น:

- Server ต้องกำลังทำงาน
- ต้องมีบัญชีที่ลงทะเบียนแล้ว

### ขั้นตอนการทดสอบ:

**ขั้นตอนที่ 1:** เข้าสู่ระบบแอปพลิเคชัน

```
ไปที่: http://localhost:5173/login
```

**ขั้นตอนที่ 2:** ไปที่หน้า Settings

```
ไปที่: http://localhost:5173/settings
```

**ขั้นตอนที่ 3:** เปิด/ปิด checkbox "รับการแจ้งเตือนทางอีเมล"

**ขั้นตอนที่ 4:** เปิด DevTools Console (กด F12) และตรวจสอบ:

```javascript
// ควรเห็น logs:
Updating settings... {settings: {emailNotifications: false}}
Update result: {user: {...}}
Profile updated successfully
```

**ขั้นตอนที่ 5:** รีเฟรชหน้าเว็บ (กด F5)

**ผลลัพธ์ที่คาดหวัง:** ✅ สถานะของ checkbox ควรคงอยู่ (ถ้าคุณยกเลิกการเลือก มันควรยังไม่ถูกเลือกอยู่)

### ตรวจสอบในฐานข้อมูล (ไม่บังคับ):

```powershell
# หากใช้ MongoDB Compass หรือ mongosh:
# ค้นหาเอกสารผู้ใช้ของคุณและตรวจสอบ:
# settings: { emailNotifications: false }
```

---

## 🔍 การทดสอบที่ 3: ความปลอดภัยในการอัพโหลดไฟล์

### กรณีทดสอบที่ 3.1: อัพโหลดไฟล์ที่ไม่ใช่รูปภาพ (ควรถูกปฏิเสธ)

**ขั้นตอนที่ 1:** สร้างไฟล์ทดสอบ text

```powershell
echo "This is not an image" > test.txt
```

**ขั้นตอนที่ 2:** ไปที่หน้ารายงาน

```
ไปที่: http://localhost:5173/report
```

**ขั้นตอนที่ 3:** ลองอัพโหลด `test.txt`

**ผลลัพธ์ที่คาดหวัง:** ❌ ควรปฏิเสธพร้อม error:

```
เฉพาะไฟล์รูปภาพเท่านั้น (JPEG, PNG, WebP, GIF)
```

---

### กรณีทดสอบที่ 3.2: อัพโหลดรูปภาพที่ถูกต้อง (ควรยอมรับ)

**ขั้นตอนที่ 1:** เตรียมไฟล์รูปภาพที่ถูกต้อง (`.jpg`, `.png`, ฯลฯ)

**ขั้นตอนที่ 2:** อัพโหลดรูปภาพ

**ขั้นตอนที่ 3:** ตรวจสอบชื่อไฟล์ใน browser DevTools Network tab

**ผลลัพธ์ที่คาดหวัง:** ✅

- ไฟล์อัพโหลดสำเร็จ
- ชื่อไฟล์ถูกสุ่ม (เช่น `a1b2c3d4e5f6...abc.jpg`)
- ไม่ใช่ชื่อไฟล์เดิม

---

## 🔍 การทดสอบที่ 4: การยกเลิก Session หลังเปลี่ยนรหัสผ่าน

### ข้อกำหนดเบื้องต้น:

- มี 2 browser sessions ที่เข้าสู่ระบบ (เช่น Chrome + Edge หรือ Chrome + Incognito)

### ขั้นตอนการทดสอบ:

**ขั้นตอนที่ 1:** เข้าสู่ระบบใน Browser 1 (Chrome)
**ขั้นตอนที่ 2:** เข้าสู่ระบบใน Browser 2 (Edge) ด้วยบัญชีเดียวกัน
**ขั้นตอนที่ 3:** ใน Browser 1 ไปที่หน้าเปลี่ยนรหัสผ่าน

```
ไปที่: http://localhost:5173/change-password
```

**ขั้นตอนที่ 4:** ขอรหัส PIN รับ OTP ทางอีเมล
**ขั้นตอนที่ 5:** เปลี่ยนรหัสผ่านสำเร็จ
**ขั้นตอนที่ 6:** ลองเข้าถึงหน้าที่ต้องยืนยันตัวตนใน Browser 2 (เช่น /profile)

**ผลลัพธ์ที่คาดหวัง:** ✅ Browser 2 ควรถูกออกจากระบบ (redirect ไปหน้า login)

---

## 🔍 การทดสอบที่ 5: XSS ในเทมเพลตอีเมล

### กรณีทดสอบ: ชื่อผู้ใช้ที่มี XSS payload

**ขั้นตอนที่ 1:** ลงทะเบียนด้วยชื่อผู้ใช้ที่มี HTML:

```javascript
ชื่อผู้ใช้: <script>alert('XSS')</script>Test
```

**ขั้นตอนที่ 2:** สร้างรายงาน (trigger email notification)

**ขั้นตอนที่ 3:** ตรวจสอบอีเมลที่ได้รับ

**ผลลัพธ์ที่คาดหวัง:** ✅ อีเมลควรแสดงข้อความที่ escape แล้ว:

```
&lt;script&gt;alert('XSS')&lt;/script&gt;Test
```

ไม่ใช่รัน script

---

## 🔍 การทดสอบที่ 6: JWT_SECRET Validation (ตรวจสอบแบบเร็ว)

### การตรวจสอบด้วยตนเอง:

**ขั้นตอนที่ 1:** เปิดไฟล์ `server\src\utils\jwt.js`

**ขั้นตอนที่ 2:** ตรวจสอบว่ามีบรรทัดเหล่านี้:

```javascript
// CRITICAL: Validate JWT_SECRET
if (!JWT_SECRET) {
  if (NODE_ENV === "production") {
    throw new Error(
      "JWT_SECRET environment variable is required in production"
    );
  }
  console.warn("⚠️  WARNING: JWT_SECRET is not set...");
}
```

**ขั้นตอนที่ 3:** รัน server และตรวจสอบ console output

**ผลลัพธ์ที่คาดหวัง:** ✅ ควรเห็นคำเตือน validation หรือเริ่มต้นโดยไม่มี error

---

## 📊 รายการตรวจสอบการทดสอบ

คัดลอกรายการตรวจสอบนี้และทำเครื่องหมายการทดสอบที่เสร็จสมบูรณ์:

```
Environment Validation:
[ ] การทดสอบที่ 1.1: การปฏิเสธ JWT_SECRET ที่หายไป
[ ] การทดสอบที่ 1.2: คำเตือน JWT_SECRET ที่อ่อนแอ

การแจ้งเตือนทางอีเมล:
[ ] การทดสอบที่ 2: การคงอยู่ของ toggle ทำงาน

ความปลอดภัยในการอัพโหลดไฟล์:
[ ] การทดสอบที่ 3.1: การปฏิเสธไฟล์ที่ไม่ใช่รูปภาพ
[ ] การทดสอบที่ 3.2: รูปภาพที่ถูกต้องพร้อมชื่อไฟล์แบบสุ่ม

ความปลอดภัยรหัสผ่าน:
[ ] การทดสอบที่ 4: การยกเลิก session หลังเปลี่ยนรหัสผ่าน

การป้องกัน XSS:
[ ] การทดสอบที่ 5: HTML entities ถูก escape ในอีเมล

JWT Validation:
[ ] การทดสอบที่ 6: ยืนยันการตรวจสอบโค้ด
```

---

## 🚀 การเริ่มต้นการทดสอบแบบเร็ว (ลำดับที่แนะนำ)

### สำหรับ Windows PowerShell:

```powershell
# 1. แก้ไข execution policy (รันในโหมด Admin)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 2. สร้าง secure JWT_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# คัดลอก output และวางลงใน server\.env

# 3. เริ่ม backend
cd server
npm run dev

# 4. ใน terminal ใหม่ เริ่ม frontend
cd ..
npm run dev

# 5. เปิด browser
# http://localhost:5173

# 6. รันการทดสอบด้วยตนเองตามคู่มือนี้
```

---

## 🐛 การแก้ปัญหา

### ปัญหา: "cannot be loaded. The file is not digitally signed"

**วิธีแก้:** รัน PowerShell ในโหมด Administrator:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### ปัญหา: ตัวแปร Environment ไม่โหลด

**วิธีแก้:** รีสตาร์ท terminal หลังจากเปลี่ยน `.env`

### ปัญหา: การเชื่อมต่อ MongoDB ล้มเหลว

**วิธีแก้:**

- ตรวจสอบ `MONGODB_URI` ใน `server\.env`
- หรือให้ใช้ in-memory DB (จะแสดงคำเตือน)

### ปัญหา: การแจ้งเตือนทางอีเมลไม่ทำงาน

**วิธีแก้:**

- ตั้งค่า `SMTP_DEV_MODE=true` ใน `server\.env` เพื่อดูอีเมลใน console แทน
- ตรวจสอบ console logs สำหรับการแสดงตัวอย่างอีเมล

---

## 📝 ทางเลือก: ใช้ Batch File สำหรับการทดสอบ

สร้าง `test-server.cmd`:

```batch
@echo off
echo Starting ScamCatcher Server...
cd server
npm run dev
```

สร้าง `test-frontend.cmd`:

```batch
@echo off
echo Starting ScamCatcher Frontend...
npm run dev
```

จากนั้นแค่ดับเบิลคลิกไฟล์ `.cmd`!

---

## ✅ เกณฑ์ความสำเร็จ

การทดสอบทั้งหมดผ่านหาก:

- ✅ Environment validation จับตัวแปรที่หายไป
- ✅ การตั้งค่าการแจ้งเตือนทางอีเมลคงอยู่หลังจาก page refresh
- ✅ ไฟล์ที่ไม่ใช่รูปภาพถูกปฏิเสธเมื่ออัพโหลด
- ✅ ชื่อไฟล์รูปภาพถูกสุ่ม
- ✅ การเปลี่ยนรหัสผ่านทำให้ออกจากระบบทุก session
- ✅ XSS payloads ถูก escape ในอีเมล
- ✅ ไม่มี console errors ระหว่างการเริ่มต้น

**หากทุกอย่างผ่าน → พร้อมสำหรับการ deployment! 🚀**
